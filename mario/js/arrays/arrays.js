var topChoices = function() {
  var myTopChoices = ["Martini", "Negroni", "Gin and Tonic", "Tom Collins"]
  for (var i = 0; i < myTopChoices.length; i++) {
    console.log("My #" + (i + 1) + " choice is " + myTopChoices[i]);
  };
}

topChoices();