<h1>Mountains</h1>
<div class="row">
  <div class="container">
    <div id="list">
    <ul>
      <% @mountains.each do |mountain| %>
        <li class="alignment"><%= link_to(mountain.name, mountain_path(mountain.id)) %></li>
      <% end %>
    </ul>
    </div>
  </div>
</div>