<% if @guess.to_i == @number.to_i %>

<p class="correct_guess">You have guessed correctly! My number is <%=@number.to_i%>
</p>

<%else%>

<p class="incorrect_guess"> Sorry! That is not the number I was thinking of! Guess again?</p>

<form action="/secretnumberguess">
    <p>
        <label for="number_guess">Enter Guess</label>
    </p>
    <p>
        <input type="text" name="number_guess" id="number_guess" placeholder="e.g. 5" required autofocus>
    </p>
    <p><button>Check Guess</button></p>
</form>

<%end%>